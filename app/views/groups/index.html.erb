<h1>Ομάδες</h1>
<%= link_to "Νέα ομάδα", new_group_path %>

<% @groups.each do |group| %>
  <div>
    <h3><%= link_to group.name, group %></h3>
    <p><%= group.users.count %> μέλη — owner: <%= group.owner.name %></p>
  </div>
<% end %>
<h1>Ομάδες</h1>
<%= link_to "Νέα ομάδα", new_group_path, class: "btn" %>

<% @groups.each do |group| %>
  <div class="row-card">
    <h3><%= link_to group.name, group %></h3>
    <p class="meta"><%= group.users.count %> μέλη — owner: <%= group.owner.name %></p>
  </div>
<% end %>